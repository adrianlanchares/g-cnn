dataset_name: crc

data_dir: data/crc
archive_dir: data/crc/archives

download: false

use_augmentation: false

data_fraction: 1.0
data_fraction_seed: 21

resize: true
image_size: 128
normalize: true
normalize_mean: [0.5, 0.5, 0.5]
normalize_std: [0.25, 0.25, 0.25]

do_validation: false
validation_split: 0.5
split_seed: 21

num_attributes: 9
