dataset_name: crc

data_dir: ${hydra:runtime.cwd}/data/crc
archive_dir: ${.data_dir}/archives

download: false

resize: true
image_size: 128
normalize: false
normalize_mean: [0.5, 0.5, 0.5]
normalize_std: [0.5, 0.5, 0.5]

do_validation: false
validation_split: 0.5
split_seed: 42

num_attributes: 9
