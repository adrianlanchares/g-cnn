defaults:
  - data: chess
  - train: default
  - model: gecnn
  - _self_

problem: chess
mode: gecnn
seed: 42

hydra:
  run:
    dir: ${hydra:runtime.cwd}/outputs/${problem}/${mode}/${now:%Y-%m-%d}/${now:%H-%M-%S}