defaults:
  - data: celeba
  - train: default
  - model: gecnn
  - _self_

problem: celeba
mode: gecnn
seed: 42

hydra:
  run:
    dir: ${hydra:runtime.cwd}/outputs/${problem}/${mode}/${now:%Y-%m-%d}/${now:%H-%M-%S}
