defaults:
  - data: chess
  - train: default
  - model: base_cnn
  - _self_

problem: chess
mode: base_cnn
seed: 42

hydra:
  run:
    dir: ${hydra:runtime.cwd}/outputs/${problem}/${mode}/${now:%Y-%m-%d}/${now:%H-%M-%S}
