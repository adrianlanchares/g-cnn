defaults:
  - data: crc
  - train: default
  - model: base_cnn
  - _self_

problem: crc
mode: base_cnn
seed: 21

hydra:
  run:
    dir: ${hydra:runtime.cwd}/outputs/${problem}/${mode}/${now:%Y-%m-%d}/${now:%H-%M-%S}
