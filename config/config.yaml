defaults:
  - data: chess
  - train: default
  - model: basecnn
  - _self_

problem: chess
mode: basecnn
seed: 42

hydra:
  run:
    dir: ${hydra:runtime.cwd}/outputs/${problem}/${mode}/${now:%Y-%m-%d}/${now:%H-%M-%S}