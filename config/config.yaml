defaults:
  - data: crc
  - train: default
  - eval: default
  - model: gecnn
  - _self_

problem: crc
mode: gecnn
seed: 21

hydra:
  run:
    dir: outputs/${problem}/${mode}/${now:%Y-%m-%d}/${now:%H-%M-%S}
