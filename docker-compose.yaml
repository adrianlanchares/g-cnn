services:
  train:
    build: .
    image: gcnn:latest
    container_name: gcnn-train
    stdin_open: true
    tty: true
    
    volumes:
      - .:/app
    working_dir: /app
    command: ["python3", "-m", "src.train"]

  train-basecnn:
    build: .
    image: gcnn:latest
    container_name: gcnn-train-basecnn
    stdin_open: true
    tty: true
    
    volumes:
      - .:/app
    working_dir: /app
    command: ["python3", "-m", "src.train", "model=base_cnn", "mode=base_cnn"]


  train-gecnn:
    build: .
    image: gcnn:latest
    container_name: gcnn-train-gecnn
    stdin_open: true
    tty: true
    
    volumes:
      - .:/app
    working_dir: /app
    command: ["python3", "-m", "src.optuna", "--study-name", "chess-gecnn"]

  tensorboard:
    build: .
    image: gcnn:latest
    container_name: gcnn-tensorboard
    stdin_open: true
    tty: true
    volumes:
      - .:/app
    working_dir: /app
    ports:
      - "6067:6006"
    command: ["tensorboard", "--logdir", "outputs", "--host", "0.0.0.0"]

  optuna-dashboard:
    build: .
    image: gcnn:latest
    container_name: gcnn-optuna-dashboard
    stdin_open: true
    tty: true
    volumes:
      - .:/app
    working_dir: /app
    ports:
      - "8067:8080"
    command: ["optuna-dashboard", "sqlite:///study.db", "--host", "0.0.0.0", "--port", "8080"]